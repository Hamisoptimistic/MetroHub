<UserControl x:Class="MetroHub.Presentation.Controls.FluentVolumeSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="16"
             MinHeight="14"
             Focusable="False"
             Cursor="Hand"
             ClipToBounds="False"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">

    <Grid x:Name="RootContainer"
          Background="Transparent"
          ClipToBounds="False"
          PreviewMouseLeftButtonDown="OnPreviewMouseLeftButtonDown"
          PreviewMouseMove="OnPreviewMouseMove"
          PreviewMouseLeftButtonUp="OnPreviewMouseLeftButtonUp"
          PreviewMouseWheel="OnPreviewMouseWheel"
          MouseEnter="OnMouseEnter"
          MouseLeave="OnMouseLeave"
          SizeChanged="OnSizeChanged"
          SnapsToDevicePixels="True">

        <!-- 1. Dark Glass Background Under-Groove (3px tall, matching Media Player) -->
        <Border x:Name="TrackTrough"
                Height="3"
                VerticalAlignment="Center"
                Background="#45000000"
                CornerRadius="1.5"
                SnapsToDevicePixels="True" />

        <!-- 2. Frosted Unfilled Track Background (3px tall, matching Media Player) -->
        <Border x:Name="TrackBg"
                Height="3"
                VerticalAlignment="Center"
                Background="#35FFFFFF"
                CornerRadius="1.5"
                SnapsToDevicePixels="True" />

        <!-- 3. Active Luminous Accent Progress Fill (3px tall) -->
        <Border x:Name="ProgressFill"
                Height="3"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Width="0"
                Background="{DynamicResource SystemAccentColorPrimaryBrush}"
                CornerRadius="1.5"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="6"
                                  ShadowDepth="0"
                                  Color="{DynamicResource SystemAccentColor}"
                                  Opacity="0.75" />
            </Border.Effect>
        </Border>

        <!-- 4. Sleek Fluent Vertical Pill Thumb (5x14px, appears on hover/drag, matching Media Player) -->
        <Border x:Name="SliderThumb"
                Width="5"
                Height="14"
                CornerRadius="2.5"
                Background="#FFFFFF"
                BorderBrush="{DynamicResource SystemAccentColorPrimaryBrush}"
                BorderThickness="1"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Opacity="0"
                SnapsToDevicePixels="True"
                IsHitTestVisible="False"
                RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <ScaleTransform x:Name="ThumbScale" ScaleX="0.6" ScaleY="0.7" />
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="8"
                                  ShadowDepth="0"
                                  Color="{DynamicResource SystemAccentColor}"
                                  Opacity="0.85" />
            </Border.Effect>
        </Border>

    </Grid>
</UserControl>
